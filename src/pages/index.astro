---
const allPosts = await Astro.glob('../pages/posts/**/*.mdx');
const MostRecentPost = allPosts.reduce((prev, curr) => Date.parse(prev.frontmatter.pubDate) > Date.parse(curr.frontmatter.pubDate) ? prev : curr);
---

<MostRecentPost.Content />